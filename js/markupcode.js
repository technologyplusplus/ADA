	
	
function MarkupPACCCode()
{

this.setCode = function ( code )
  {
  return "<h3>" + code + "</h3>";
  }	
	
this.setTitle = function( title )
	{
	return "<h3>" + title + "</h3>"; 
	}		

this.setType = function( classtype )
  {
	return "<p>" + classtype + "</p>";
	}
	
this.setPenalty = function( penalty )
  {
	return "<p>" + penalty + "<p>";
	}
	
this.setNote = function( note )
  {
	return "<p>" + note + "<p>";
	}
	
this.setChangeDate = function( date )
  {
	return "<p>" + date + "<p>";
  }	
	
this.setResponsibility = function( respon )
  {
	return "<p>" + respon + "<p>";
	}

this.setLanguage = function( lang )
  {
  return "<p>" + lang + "<p>";
  }	
	
this.setGroup = function( group )
  {
  return "<p>" + group + "<p>";
  }		
	
this.setCodeClass = function( codeclass )
  {
  return "<p>" + codeclass + "<p>";
  }		
	
this.setSpecialCode = function ( specode )
	{
  return "<p>" + specode + "<p>";
  }		
	
	
	
	
	
}
